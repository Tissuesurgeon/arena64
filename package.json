{
  "name": "arena64",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=arena64-web",
    "dev:api": "cd apps/api && uvicorn app.main:app --reload --port 8000"
  }
}